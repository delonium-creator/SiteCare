# Заметки о проекте и процессе работы (Claude Cowork)

Обновлено: 2026-08-16

## Что это за файл

Журнал решений и текущего состояния, чтобы не терять контекст между сессиями чата с Claude.
Технические детали самого продукта — в `PRODUCT-SPEC-RU.md` и `README-RU.md`, здесь только
про процесс разработки/деплоя.

## Текущее состояние репозитория

- Репозиторий: https://github.com/delonium-creator/SiteCare (публичный)
- Ветка `main`, последний осмысленный коммит на момент записи — про `.dev.vars` scaffold.
- Локальный запуск (`npm run dev`, `wrangler dev`) и деплой пока не настроены как
  автоматический процесс — используются вручную с компьютера разработчика.

## Известные ограничения облачной Cowork-сессии (не баги проекта!)

Эти вещи не работают из облачной сессии Claude (Home/Cowork) и это не связано с кодом проекта:

1. **`npm install` не может поставить `miniflare`/`wrangler`** — сетевая политика песочницы
   блокирует часть пакетов npm-реестра (`403 host_not_allowed`). Из-за этого 3 тестовых файла
   (`gateway.test.js`, `integration.test.js`, `platform.test.js`) не запускаются в облаке —
   на обычном компьютере или в GitHub Actions они отрабатывают нормально.
1a. **На локальной Windows-машине те же 3 файла падают иначе** — `npm install` проходит, но
    сами тесты валятся с `Error: write EOF` (обрыв IPC-канала до подпроцесса `workerd`,
    который поднимает Miniflare для эмуляции D1). Проверено 2026-08-16: ошибка стабильно
    воспроизводится на каждом прогоне, всегда в одних и тех же тестах, без единого
    assertion-провала — то есть это несовместимость Miniflare/workerd с этой конкретной
    Windows-средой, а не баг кода. Надёжный сигнал по этим файлам — только GitHub Actions
    (`ubuntu-latest`), локально на Windows им доверять не стоит. `node --test --test-force-exit`
    даёт увидеть детали ошибки вместо зависания, но саму проблему не решает.
2. **`git push` из облачной сессии не работает вообще**, даже с personal access token —
   известный открытый баг Anthropic: proxy блокирует пуш в репозитории, не входящие
   в "authorized repository set" сессии, обхода пока нет.
   Issue: https://github.com/anthropics/claude-code/issues/76248
   Из-за этого изменения из облачной сессии в GitHub переносятся вручную (см. ниже).
3. Один тест (`assets.test.js` — "Windows launcher stays ASCII with CRLF") падает в Linux-среде,
   потому что `START-SITECARE.bat` сохранён в git с LF, а тест ожидает CRLF. На Windows это
   не всплывает благодаря `core.autocrlf`. Не исправлено — можно добавить `.gitattributes`
   (`*.bat text eol=crlf`), если понадобится тест зелёным везде.

## Как переносить изменения из облачной сессии в GitHub (пока баг с пушем не пофиксили)

- Маленькая правка (1-2 файла) — скопировать готовое содержимое файла и вставить через
  веб-редактор GitHub (Edit → Commit).
- Много файлов — Claude собирает их в zip, присылает файлом; на github.com:
  Add file → Upload files → перетащить содержимое zip → Commit.
- После переноса в облачной сессии сделать `git pull`, чтобы синхронизироваться.

## План перехода на автоматический деплой (GitHub Actions)

Workflow-файл `.github/workflows/ci-cd.yml` уже подготовлен (тесты на каждый push/PR,
деплой обоих воркеров при пуше в `main`). Он лежит локально в облачной сессии и ещё
**не перенесён на GitHub** — сделать это тем же способом (upload zip), что и с этим файлом.

Что нужно сделать один раз (руками, на GitHub и Cloudflare), чтобы деплой стал полностью
автоматическим и больше не требовал ни компьютера, ни ручного `wrangler`:

- [x] Перенести `.github/workflows/ci-cd.yml` на GitHub — файл лежал в корне репозитория
      и не подхватывался Actions; перенесён в `.github/workflows/ci-cd.yml` (2026-08-16).
- [x] Создать реальные D1-базы на Cloudflare и вписать их id вместо заглушек —
      оба id уже были созданы (`cdf0bcc2...` для основной БД, `4b2adca1...` для gateway),
      но после ручной загрузки файлов через веб-интерфейс GitHub перепутались местами:
      конфиг шлюза оказался в корневом `wrangler.jsonc` вместо основного воркера,
      а основной конфиг — в лишнем файле `wrangler (1).jsonc`. Разобрано и исправлено
      (2026-08-16): корневой `wrangler.jsonc` снова содержит конфиг основного воркера,
      `gateway/wrangler.jsonc` — конфиг шлюза с реальным id вместо `PUT_GATEWAY_DATABASE_ID_HERE`.
- [ ] В настройках репозитория GitHub → Settings → Secrets and variables → Actions
      добавить 4 секрета: `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`,
      `OPENAI_API_KEY`, `TELEGRAM_BOT_TOKEN` (вводить прямо на GitHub, не в чат)

После этого: любой пуш в `main` сам прогоняет тесты и деплоит оба воркера на Cloudflare —
проект "запускается" уже не локально, а как боевой сервис (Cloudflare Worker + Telegram-бот),
работающий постоянно, без участия компьютера разработчика.

## Перенос правок из архива 7.0.3 (2026-08-16)

Пользователь предоставил отдельно скачанный архив `sitecare-tilda-final-v7.0.3-ready.zip` —
версия новее git-baseline (7.0.0), с реальными исправлениями дизайна, которые обсуждались
в старых чатах, но так и не попали в этот репозиторий. Побайтовое сравнение (после
нормализации CRLF/LF) показало: 7.0.3 и текущий репозиторий — **две разошедшиеся ветки**
от общего предка, не одна лучше другой. У 7.0.3 есть доработки интерфейса, которых нет
у нас; у нас — health score/дайджест/отзывы, которых нет в 7.0.3.

Перенесено в `gateway/src/platform-ui.js`:
- единое оформление логотипа (`.wordmark`) на всех экранах — крупный, по центру, без
  отдельной версии рядом (`.version{display:none!important}`), включая свёрнутое меню;
- `aria-label="SiteCare"` на логотипе (доступность);
- согласованный layout «Главной»: `.editor-layout` (компактная левая колонка ~32% +
  чат ~68%), функция `changeHistoryRows()`, компактный блок `.compact-history` с
  тремя последними изменениями и кнопкой «Показать всю историю» (открывает диалог
  через новый `document.addEventListener` на `history-dialog`) — вместо отдельного
  блока на всю ширину внизу экрана, который был раньше.

Сознательно НЕ перенесено:
- **редирект `/admin` → `/app`** (функция `centralAppUrl()` в `src/index.js`) — по
  просьбе пользователя: сайт «Салон Verme» сейчас используется как тестовый шаблон
  через старый `legacy`-кабинет, и редирект на центральную платформу сломал бы этот
  сценарий (там нет `central`-аккаунта для этого сайта). Переносить, когда появится
  реальный клиент на `central`-режиме.
- доработка `renderOperatorOverview` (классы `.operator-focus-metrics`/`.operator-focus-list`,
  обёртка `baseRenderOperatorOverviewV65` в 7.0.3) — не тронуто: не связано с жалобой
  пользователя, риск сломать операторскую панель без возможности протестировать логином.
- `<span id="panelVersion">` не удалён из HTML — просто гарантированно скрыт через CSS
  (`.version{display:none!important}`), чтобы не трогать JS, который на него ссылается
  (`classList.add/toggle('hidden', ...)`).

Все 101 тест, не зависящий от Miniflare, зелёные после переноса (включая оба
`platform-ui.test.js`). Три Miniflare-зависимых файла по-прежнему не проверить локально
на этой Windows-машине (см. пункт про `write EOF` ниже).

## Три реальных бага после живого теста виджета (2026-08-16)

Пользователь протестировал задеплоенный виджет на реальном сайте и прислал скриншоты трёх
настоящих проблем (не косметика — это упустил при верификации, хотя должен был поймать):

**1. Диалог «залипал» на предыдущей теме.** После вопроса про телефон без ответа
(незавершённый `dialog`) следующее сообщение — даже «Проверь состояние сайта» — снова
получало «Какой телефон вы хотите изменить?». Причина: `continueDialog()`
(`platform-assistant.js`) при НЕудачном разборе ответа просто переспрашивал тот же
вопрос вместо проверки, относится ли новое сообщение к теме вообще. Это ровно та же
проблема, что упоминалась в самой первой истории проекта (см. «Этап 2» в разделе про
восстановленную историю продукта) — просто её так и не победили. Исправлено: все
тупиковые ветки `continueDialog` (phone: stage target/location/value; button: stage
attribute/value) при нераспознанном ответе теперь возвращают `null` — это сигнал
«не продолжение диалога», и `prepareSiteChange()` обрабатывает сообщение как новое,
свежее обращение вместо зацикливания.

**2. «Выбрать на сайте» реально не работал.** Клик по номеру на опубликованной странице
просто закрывал вкладку и ничего не происходило. Вероятная причина — `window.opener`
ненадёжен для КРОССдоменных всплывающих окон: у многих сайтов (в т.ч., похоже, у Tilda)
настроена `Cross-Origin-Opener-Policy`, которая молча обнуляет `window.opener` для
попапов на чужой домен — `postMessage` в таком случае просто ничего не делает, ошибки
нет. Исправлено добавлением надёжного запасного канала связи, НЕ зависящего от
`window.opener`:
- новая таблица `platform_pending_selections` (миграция `0017_pending_selection.sql`) —
  однострочное хранилище на сайт, с TTL 10 минут;
- новый публичный эндпоинт `POST /v1/public/sites/:id/select?key=...` — загрузчик
  репортит клик туда напрямую (та же схема авторизации `key`+`Origin`, что у `/applied`);
  `postMessage` остался как best-effort резерв, но не единственный путь;
  новый авторизованный эндпоинт `GET /v1/platform/sites/:id/selection` — панель
  проверяет его при возврате фокуса на вкладку (`window.addEventListener('focus',...)`),
  что не зависит от `window.opener` вообще — просто "мы снова активны, есть что забрать?".
- Проверено: реальный fetch с тестовой страницы на `/select` (лог сервера подтвердил
  сохранение), реальный GET `/selection` вернул тот же payload. Живую двухоконную
  склейку (`window.open` → реальный фокус) в этой тестовой среде эмулировать не удалось
  (тот же нюанс автоматизации, что и раньше — попап не отслеживается как отдельная
  вкладка), но обе половины цепочки проверены раздельно и по-настоящему.

**3. Баннер режима выделения перекрывал шапку сайта.** Был `position:fixed;top:0`
на всю ширину — переделан в компактную плашку снизу по центру
(`position:fixed;left:50%;bottom:20px;transform:translateX(-50%)`), чтобы не закрывать
навигацию сайта независимо от того, как он свёрстан.

Заодно по тому же сообщению — сжатый композер и подсказки в виджете (`chat-suggestions`
теперь горизонтальный скролл в одну строку вместо стека на весь экран, `chat-send`
без текстовой подписи, только иконка), более плавная анимация раскрытия
(`cubic-bezier(.34,1.4,.64,1)`, старт с `scale(.3)` вместо `scale(.85)` — заметнее
эффект «разворачивания»), кнопка расширения виджета на весь экран (`.chat-widget.expanded`).

Пользователь справедливо отметил, что часть этого стоило поймать до показа, а не после
его сообщения — особенно баннер поверх шапки и «залипание» диалога, оба видны при
минимальном тестировании реального сценария использования, а не только структуры HTML.

## Исправление вёрстки плавающего чата (2026-08-16, после первого деплоя виджета)

Сразу после деплоя плавающего чата пользователь прислал скриншоты: текст в шапке виджета
переносился по одному слову на строку, читать невозможно. Причина найдена измерением
реальных ширин через JS в браузере (не на глаз): `.chat-panel-head` — `display:flex`
контейнер с `.chat-identity` (`display:grid`, без явного `flex-grow`) и `.chat-presence`
(бейдж-пилюля, `flex:0 0 auto`, не сжимается). Вся эта раскладка была рассчитана на широкую
панель (800+px в исходном полноэкранном режиме) — в узком виджете (400px) `.chat-identity`
сжимался под нерастягивающийся бейдж, а его `grid-template-columns:76px minmax(0,1fr)`
схлопывал текстовую колонку до ~120px. Заголовок раздувался до 413px высотой, ломая всё
остальное (зона сообщений сжималась до неприличного минимума).

Исправлено — не попыткой донастроить старую раскладку, а отдельным компактным заголовком
специально для виджета (`.chat-widget-head`, простой flex-ряд: аватар 36px + имя + статус
+ крестик), а исходный `.chat-panel-head` внутри виджета скрыт через `.chat-widget
.chat-panel-head{display:none}` (сам HTML не трогали, чтобы не дублировать logic сборки
`assistant` markup). Высота заголовка — 67px вместо 413px.

Заодно по тому же сообщению:
- Анимация раскрытия/закрытия: иконка-пузырь схлопывается в точку (`scale(0)`) при открытии
  чата и возвращается при закрытии — не настоящий shared-element morph, а два синхронно
  анимированных элемента с общей точкой отсчёта (`transform-origin:bottom right`), но
  воспринимается как единое целое.
- Автопрокрутка чата вниз при каждом открытии/обновлении (`box.scrollTop=box.scrollHeight`
  после `innerHTML` в `renderChatWidget()`) — раньше открывался с прокруткой наверх.
- Убрана дублирующая кнопка «Спросить помощника о причинах» на странице диагностики
  (была нерабочей, если виджет закрыт — `$('changePromptV45')` не существовал).
- Плашки индекса/замечаний получили подписи текстом («Индекс здоровья: 92»,
  «5 замечаний») вместо голых чисел без контекста.
- Галочки статусов (сайт/заявки/Telegram) увеличены до 30px в кружке.

Не сделано (сознательно, обсуждалось отдельно): полноценный визуальный редизайн «под
современность» — точечные фиксы выше нацелены на конкретные жалобы, не тотальная смена
стиля; перепроверка качества реальных ответов OpenAI (ключ настроен, но живой диалог не
тестировался программно, только вручную described-by-user).

## Плавающий чат + «Диагностика» вместо «Главной» (2026-08-16)

По просьбе пользователя чат-помощник вынесен из основного экрана в отдельный плавающий
виджет (иконка-робот, фиксирована справа внизу) — доступен с любого клиентского экрана
(Диагностика/Заявки/Отзывы/Настройки), не показывается оператору вне режима предпросмотра
клиента. Состояние открыт/закрыт (`chatWidgetOpen`) сохраняется при переключении вкладок
(`render()` вызывает `renderChatWidget()` в конце, независимо от `currentView`). Закрывается
крестиком или Escape.

`renderClientEdit` (вкладка «Главная») переименована в «Диагностика», рендерит только
блок состояния сайта и историю изменений — на всю ширину (класс `.diagnostics-only`,
max-width:980px), без урезанной колонки под чат. Карточки статуса (сайт/заявки/Telegram)
переведены на адаптивную сетку `repeat(auto-fit,minmax(230px,1fr))` — на всю ширину
выстраиваются в 3 колонки вместо вертикального стека.

Механика клика по логотипу помощника внутри диагностики теперь просто напоминает,
что открывается плавающий чат («Спросить помощника о причинах» → «Откроется чат в
плавающем окне справа»), сам чат больше не встроен в этот экран.

Обработчики `data-editor-action`/`data-prompt-example` переведены с `$('view')` на
`document` (делегирование), поскольку виджет чата находится вне `#view`.

Один тест (`platform-ui.test.js`) проверял старые литеральные строки интерфейса
(подзаголовок «Главной», `class="editor-layout"`) — обновлён под новую структуру.

Не сделано: перенос выбора кнопок кликом по сайту (только телефон, см. предыдущий
пункт); горячие клавиши для самой диагностики (сделан только Escape для закрытия чата).

## Точечный выбор номера телефона + «Выбрать на сайте» (2026-08-16)

Пользователь много раз жаловался, что замена телефона работает непредсказуемо: когда на
странице несколько номеров (особенно одинаковых, например плейсхолдер `111111111111111` в
двух разных местах), система не даёт выбрать конкретное место — предыдущая сессия пыталась
это решить (есть мёртвый код `choosePhoneLocation()` в `platform-assistant.js`), но не довела
до конца.

Найдена настоящая причина — в `applyPreparedChange()` (`gateway/src/platform.js`) при
применении смены телефона `scope` был **жёстко зашит как `"site"`** (значит «везде»),
несмотря на то, что вся остальная инфраструктура (запись `platform_phone_target_rules`,
применение в загрузчике через `blockId`/`occurrenceIndex`) уже полностью поддерживала
точечный `scope:'element'`. Клиент (`platform-ui.js`) при этом уже правильно отправлял
`scope` в запросе — сервер его просто игнорировал. Та же жёсткая подстановка была и в
`continueDialog()` при продолжении диалога после выбора места.

Исправлено:
- `platform.js` — `scope` теперь читается из `body.scope` (валидируется по тому же
  паттерну, что уже использовался для кнопок), по умолчанию `"site"` для обратной совместимости.
- `platform-assistant.js` — `continueDialog` сохраняет `dialog.scope`, если исходный
  кандидат по-прежнему находится в свежем сканировании; иначе безопасно откатывается на `"site"`.

Добавлена новая функция — **клик прямо по сайту** вместо угадывания по тексту:
- Новый эндпоинт `POST /v1/platform/sites/:id/assistant/locate-phone` — принимает
  `{pagePath, blockId, source, occurrenceIndex, originalDigits}` от клика, сам сканирует
  сайт свежим `scanSiteInventory()` и находит точное совпадение (не полагается на
  клиентский `candidateId`, чтобы не зависеть от рассинхронизации хэшей между сканами).
- `gateway/src/platform-core.js` (`sitecareLoaderRuntime`) — режим выделения по параметру
  `?sitecare_select=phone` в URL: подсвечивает все `tel:`-ссылки и текстовые совпадения на
  странице, по клику отменяет переход и шлёт `window.opener.postMessage({channel:'sitecare-select',...})`.
- `platform-ui.js` — кнопка «Выбрать прямо на сайте» в диалоге уточнения телефона
  (`openSiteSelector()`, открывает сайт в именованном окне `sitecare-select`), приём
  сообщения (`window.addEventListener('message', ...)`, проверка `event.origin`), дальше
  идёт как обычный ответ ассистента.

Проверено: 21 тест (`platform.test.js` + `platform-ui.test.js`) зелёные; отдельно собранный
`sitecare-loader.js` (через `loaderJavascript()`) синтаксически корректен; вручную прогнан
полный сценарий на локальном моке — клик по номеру в подвале тестовой страницы отправил
верный payload (`blockId:"rec300"`, `source:"text"`, `originalDigits:"74952480352"`).

Сознательно не сделано: выбор кнопок кликом (только телефон в этой итерации — кнопки
используют другую систему идентификации через `candidateId`-хэш и требуют отдельного
похожего, но не идентичного решения); переработка навигации на «Диагностика» и вынос чата
в отдельное всплывающее окно (обсуждались отдельно, не начаты).

## Деплой 2026-08-16

Впервые задеплоен весь функционал health score / дайджеста / отзывов, который до этого
только лежал в git с 2026-08-14 и не публиковался:

- Применены миграции `0014_health_score.sql`, `0015_digests.sql`, `0016_review_sources.sql`
  на боевую базу шлюза (`sitecare-telegram-gateway-db`).
- Задеплоен основной воркер (`sitecare-ketedes-page169452909`, version `90cc6dfa`).
- Задеплоен воркер-шлюз (`sitecare-telegram-gateway`, version `b4b70746`).
- Секреты (`OPENAI_API_KEY`, `TELEGRAM_BOT_TOKEN` и другие) уже были настроены заранее —
  проверено через `wrangler secret list`, ничего нового не вводилось.
- Заодно поправлен `test/deploy.test.js`: тест ждал ровно placeholder-строку
  `https://PUT-YOUR-GATEWAY.workers.dev` в `TELEGRAM_GATEWAY_URL`, хотя сам код
  (`validateProjectConfig`) уже давно принимает и placeholder, и реальный адрес по regex —
  тест переведён на ту же проверку по regex вместо точного совпадения с заглушкой.

## История продукта (восстановлено из внешних переписок, 2026-08-16)

Git-история репозитория начинается с коммита «Baseline: SiteCare 7.0.0 as delivered» —
всё, что было до него, велось в чатах вне репозитория и здесь не сохранялось. Восстановлено
из общих ссылок на ChatGPT и переписки в этой же сессии Claude, чтобы не потерять контекст:

**Этап 1 (ChatGPT, версии ~4.3.0 → 6.0).** Макеты панелей клиента/админа готовились
картинками до кода. Найдены и исправлены: гигантский чекбокс (CSS от текстового поля),
модалка закрывалась по клику на фон и теряла несохранённые данные, сессия жёстко истекала
через 12 часов без продления. Ключевой архитектурный поворот — отказ от ручного
конструктора редактирования «из десятка полей» в пользу диалога с ассистентом (спрашивает,
какой элемент менять, показывает карточку «сейчас → будет»). Раздел заявок переделан из
технического `form316328935` в понятную карточку (имя/телефон/почта/UTM, шифрование,
статусы). Обнаружен и исправлен критичный баг: правки показывали «успех», хотя на
опубликованном сайте ничего не менялось.

**Этап 2 (ChatGPT, версии 6.3.0 → 6.4.0).** Ассистент «залипал» на прошлой задаче — после
применения правки следующая фраза ошибочно считалась продолжением той же правки; исправлено
очисткой состояния диалога. Для одинаковых элементов (несколько одинаковых номеров/кнопок)
добавлены карточки с точным расположением + клик прямо по опубликованному сайту. Ужесточено
распознавание телефонов (техническими ID Tilda раньше ложно считались номерами). Убраны
дублирующиеся кнопки, кнопка «Назад», отдельная вкладка уведомлений, лента событий. Починена
панель поддержки: оператор раньше не мог сам ничего менять, мог заспамить клиента повторным
«Взять в работу», видел чужие внутренние сообщения после закрытия обращения.

**Этап 3 (эта же сессия Claude, привела к текущим коммитам `0c861c3`…`ba127f5`).**
Health score и дайджед: — тот код, что сейчас в `gateway/src/platform-monitor.js`.
Модуль отзывов решён без скрейпинга и хранения: админ вставляет в панели поддержки
`src`-ссылку из официального виджета Яндекс.Карт/2ГИС («Поделиться → Виджет с отзывами» /
«Отзывы → Виджет с рейтингом»), клиент видит готовую витрину без элементов управления.
Freshness-напоминания («цены/график не менялись N месяцев») сознательно отложены — не
реализованы. Чистка ~63 неиспользуемых CSS-классов не доведена до конца — селекторы
склеены с живыми классами (`.metric,.card,.status-tile{...}`), объединены только
дублирующиеся `:root`-блоки (коммит `a759432`). Ничего не задеплоено в Cloudflare —
только локальная проверка (139 тестов).

## Про вкладку "Code" в приложении Claude

Отдельная от Cowork/Home сессия, работает через Remote Control, привязанный к конкретному ПК.
Не путать с этой (облачной) сессией — они не видят состояние друг друга.
На момент записи там наблюдалась ошибка `403 API Error: Failed to authenticate` — не связана
с git/GitHub, похоже на проблему авторизации самой сессии Code, не диагностирована до конца.

## Чат-виджет: доведение до рабочего состояния (2026-08-16, третий раунд правок)

После переноса чата в плавающий виджет и рескейла интерфейса пользователь сообщил три новых
симптома: при каждом новом сообщении «чат скроллится сверху вниз», быстрые кнопки вставляли
текст в чат вместо мгновенного срабатывания, «все кнопки не работают». Разобрано и исправлено:

- **Корневая причина «одинакового общего ответа дважды подряд»** (жалоба из предыдущего
  раунда — на «Проверь состояние сайта» ассистент дважды отвечал «Уточните, пожалуйста, что
  нужно изменить»): в `platform-assistant.js` цепочка `prepareSiteChange()` уходит
  OpenAI → Cloudflare Workers AI (`askAi`/`normalizeAiProposal`) → generic fallback. По факту
  в проде срабатывает besplatная Cloudflare-модель (`@cf/zai-org/glm-4.7-flash` и т.п.), которая
  не распознаёт даже простые статусные вопросы и падает в fallback-текст
  `normalizeAiProposal()`. Сам вызов OpenAI, похоже, не отрабатывает или не настроен для этого
  класса вопросов — не чинили сам классификатор (это отдельная, более крупная задача), вместо
  этого закрыли конкретный класс жалоб точечно (см. ниже).
- **`continueDialog()` в `platform-assistant.js`** на нераспознанном ответе повторно задавал
  тот же уточняющий вопрос («Какой телефон вы хотите изменить?» и т.п.) вместо того, чтобы
  пропустить реплику дальше на свежую обработку — из-за этого диалог мог «залипать» на старом
  вопросе, если клиент фактически спросил что-то другое. Все тупиковые ветки (телефон:
  target/location/value, кнопка: attribute/value) теперь возвращают `null` при непонятном
  ответе и падают в обычный pipeline вместо переспроса.
- **Быстрые кнопки предложений (`assistantSuggestions()` в `platform-ui.js`)** переведены с
  `data-prompt-example` (подставить текст в поле и отправить через AI) на
  `data-editor-action="quick-*"` с детерминированной обработкой в новых `handleQuickAction()`/
  `injectQuickAnswer()`: «Проверь состояние сайта» дергает `/check` и строит ответ из
  `pageOk`/webhook-статуса/Telegram напрямую; «Проведи SEO-диагностику» и «Что можно улучшить?»
  берут `diagnostics.issues` из `/inventory`; «Почему могут не приходить заявки?» — чистая
  локальная логика по `form_required`/`webhook_verified_at`/`form_verified_at`. Все четыре не
  трогают AI-пайплайн вовсе, поэтому не зависят от качества классификации бесплатной модели.
  Кнопки, предложенные самим ассистентом в ходе диалога (не из дефолтного набора), по-прежнему
  идут через `data-prompt-example` — переделаны только 4 стартовые.
- **Найден и исправлен реальный баг** в первой версии `handleQuickAction`: обработчик
  `quick-check` вызывал `await load(accountId)` — полный перезапрос дашборда, который создаёт
  новые объекты сайтов из JSON и стирает клиентские поля `_conversation`/`_overrides`
  (привязанные к старой ссылке на объект). Из-за этого виджет вместо вставленного ответа
  показывал заглушку «Открываю помощника…». Исправлено: `load()` убран, вместо него
  `s.page_ok`/`s.tls_ok`/`s.loader_ok` обновляются напрямую из camelCase-полей ответа `/check`
  без потери ссылки на объект. Обнаружено и проверено вживую через локальный мок-сервер (не
  только чтением кода) — сетевой запрос уходил верно, но UI не обновлялся, пока не убрали
  `load()`.
- **Высота и скролл**: `.chat-widget` был с фиксированной `height:min(600px,...)`, из-за чего
  контейнер `.assistant-chat` со скроллом всегда занимал одну и ту же высоту, даже когда
  сообщений мало — визуально выглядело как «скролл на пустом чате». Заменено на
  `height:auto;max-height:min(600px,calc(100vh - 60px))`, `.assistant-chat` — на
  `flex:1;min-height:0;overflow-y:auto`: виджет растёт вместе с сообщениями и включает скролл,
  только когда контент реально превышает потолок. Проверено вживую: с одним приветствием —
  505px без скролла; после одного ответа — 476px, всё ещё без скролла; после 4 обменов
  репликами — упирается в потолок 600px и появляется скролл, как и задумано.
- **Скролл к последнему сообщению** переведён с прямого `box.scrollTop=box.scrollHeight` (не
  успевал сработать до перерисовки DOM) на двойной `requestAnimationFrame(...)`, чтобы браузер
  успел применить layout перед скроллом.

Проверено вживую на локальном моке (`node:http` сервер, отдающий реальный `platformHtml()`):
все 4 быстрые кнопки, кнопка отправки в композере (POST на `/assistant`, ответ с `conversation`
вставляется в виджет), кнопка расширения (`.expanded`, ширина 400px → 920px и обратно),
кнопка закрытия (виджет скрывается, пузырь остаётся видимым) и повторное открытие через
пузырь — всё отрабатывает корректно. Полный набор тестов после правок: 139/139 зелёных
(`node --test test/*.test.js`, включая Miniflare-тесты — на этой машине уже отрабатывают
чисто после установки VC++ Redistributable).

Сознательно не сделано: сам классификатор AI для произвольных текстовых вопросов (не входящих
в 4 быстрые кнопки) остаётся зависимым от бесплатной Cloudflare-модели и может по-прежнему
путаться — не переделывали, это отдельная, более крупная задача (либо чинить вызов OpenAI,
либо резко расширять локальный классификатор). Общая визуальная модернизация интерфейса
(«выглядит как в 2010 году», отдельная жалоба из предыдущего раунда) не начата — не в скоупе
точечных фиксов этого раунда.

## Настоящая причина «чат постоянно скроллится сверху вниз» и диагностика по кнопке (2026-08-16)

После деплоя предыдущего раунда пользователь сообщил, что чат по-прежнему «постоянно
скроллится сверху вниз», и попросил убрать «Индекс здоровья» с страницы «Диагностика»,
заменив автоматический список проблем на кнопку, запускающую полную проверку.

- **Настоящая причина скролла**: класс `.assistant-chat` (используется и в чате-виджете, и
  в старом полноэкранном чате) имеет `scroll-behavior:smooth`. Каждый ре-рендер виджета
  (`$('chatWidget').innerHTML=...`) создаёт новый DOM-узел `.assistant-chat` со `scrollTop=0`,
  и любая последующая программная установка `scrollTop` (в т.ч. предыдущий фикс с двойным
  `requestAnimationFrame`) анимируется браузером из-за `smooth` — визуально это и есть «скролл
  сверху вниз». А происходит это не только при новом сообщении: `render()` (который всегда
  вызывает `renderChatWidget()`) дёргается фоновыми `setInterval`-опросами каждые 9–12 секунд
  (ожидание применения правки, обновление диалога с поддержкой) — то есть скролл-анимация
  срабатывала регулярно сама по себе, без действий пользователя. Исправлено: у
  `.chat-widget .assistant-chat` явно выставлен `scroll-behavior:auto`, плюс сам
  `renderChatWidget()` теперь запоминает, было ли положение прокрутки внизу и вырос ли список
  сообщений (`chatLastMessageCount`) — прокрутка к последнему сообщению происходит только при
  реальном новом сообщении или если пользователь и так был внизу; в остальных случаях позиция
  прокрутки восстанавливается как была, а не сбрасывается наверх.
- **«Индекс здоровья» убран** со страницы «Диагностика» (сам показатель `health_score` в БД
  и API не тронут — используется в дайджестах, просто не выводится этим бейджем).
- **Список проблем сайта переведён на ручной запуск.** Раньше «критические» проверки
  (TLS/доступность/код SiteCare/приём заявок) считались и показывались всегда, а более
  подробный SEO/доступность-скан подтягивался только если где-то уже был вызван `/inventory`
  (например, через кнопку в чате). Теперь на странице по умолчанию — карточка-приглашение с
  иконкой и кнопкой «Запустить диагностику»; сами три быстрых индикатора (сайт/заявки/Telegram)
  остаются видны всегда как и раньше. По нажатию кнопка показывает анимированное состояние
  «Анализирую сайт…» (вращающаяся SVG-иконка), параллельно вызывает `/check` и `/inventory`,
  затем показывает полный список замечаний (критические + SEO/доступность из
  `diagnostics.issues`) с плавным поочерёдным появлением карточек (`nth-child`-задержки в
  CSS-анимации). После первого запуска кнопка превращается в «Обновить диагностику». Как и в
  прошлом фиксе quick-check, обновление `page_ok`/`tls_ok`/`loader_ok` идёт прямым мутированием
  полей объекта сайта — без `load()`, чтобы не терять состояние переписки в чат-виджете.
- **Добавлено логирование ошибок OpenAI**: `catch` вокруг вызова `requestOpenAiAssistant()` в
  `prepareSiteChange()` (`platform-assistant.js`) раньше полностью проглатывал ошибку без
  единого следа. Добавлен `console.error('openai_assistant_failed', ...)` с сообщением,
  HTTP-статусом и `request-id` — это не меняет поведение, но впервые даёт возможность увидеть
  в `wrangler tail`, почему запрос к OpenAI не сработал.

**Про жалобу «AI вообще ничего не понимает» — расследование, не доведено до конца.**
Прямой запрос к боевой базе (`platform_conversation_messages` в `sitecare-telegram-gateway-db`)
показал: почти все ответы ассистента за 16.08.2026 (18:32–19:31) имеют
`type:"clarification", kind:"unknown"` — то есть попадают в обобщённый fallback-путь, а не
в реальную классификацию. Дневная квота AI-запросов (`platform_usage_daily`) при этом не
исчерпана (5 из 20 на starter-плане) — значит, дело не в лимите. `env.OPENAI_API_KEY` и
`env.OPENAI_MODEL=gpt-5-mini` привязаны к воркеру (видно в выводе `wrangler deploy`), то есть
код должен пытаться сначала звать OpenAI. Рабочая гипотеза: вызов `requestOpenAiAssistant()`
либо падает с ошибкой (сеть/авторизация/формат), либо превышает таймаут, и код молча
проваливается в бесплатный Cloudflare Workers AI (`@cf/zai-org/glm-4.7-flash` и т.п.), который
на свободных вопросах часто возвращает `kind` не из ожидаемого набора или вообще не
`"advice"`, из-за чего пользователь получает шаблонное «Уточните, пожалуйста, что нужно
изменить» почти на любой вопрос, не являющийся точной командой на замену телефона/графика/
кнопки. Логирование (см. выше) добавлено именно для проверки этой гипотезы, но подтвердить
её `wrangler tail`-логами в этой сессии не удалось: у сессии нет пароля/сессии от боевого
кабинета клиента, чтобы самостоятельно отправить тестовое сообщение в чат (и не должно быть —
ввод паролей за пользователя запрещён политикой безопасности сессии). Нужен один реальный
запрос от пользователя в живом чате при включённом `wrangler tail`, чтобы увидеть точную
причину и решить: чинить вызов OpenAI (не исключено, что дело в имени модели `gpt-5-mini` —
это не проверялось напрямую) или значительно переписывать сам промпт/классификатор.

## Диагноз OpenAI подтверждён и закрыт: баланс, не код (2026-08-17)

Пользователь прислал реальный вопрос через живой чат при включённом `wrangler tail` — лог
показал точную причину: `openai_assistant_failed OPENAI_RATE_LIMIT 429`. У OpenAI под кодом 429
скрываются и настоящий rate-limit, и «закончилась квота/не привязана оплата» — по характеру
(одинаковый провал на каждом запросе за день, а не изредка) это однозначно нехватка баланса,
а не частота запросов. Модель `gpt-5-mini` тут ни при чём — она валидна и по сей день значится
в официальном прайсе OpenAI ($0.25/1M вход, $2.00/1M выход).

Обсудили с пользователем реальную стоимость (по факту — копейки при текущей нагрузке, $1-2/мес
по прикидке) и способы пополнения из РФ (карта не проходит напрямую; разбирали внешний
реселлер API-ключей и «прямое пополнение чужой картой» — оба варианта отклонены как рискованные:
доступ к чужому аккаунту = риск потери уже рабочего продакшен-ключа, чужая ворованная карта =
шанс чарджбэка и списания долга на пользователя задним числом уже после траты баланса;
легитимный путь — зарубежная виртуальная карта вроде pipl.io). Пользователь решил: **пока не
пополнять OpenAI вообще** — соотношение "разбираться с картой ради AI, который сейчас не
критичен для задачи" не оправдано. Явное указание: доработать бесплatную связку (Cloudflare
Workers AI + локальный классификатор) вместо оплаты OpenAI. К вопросу об OpenAI возвращаться
только если пользователь попросит явно.

## Доработка бесплатного чат-помощника «под ключ» (2026-08-17)

По итогам решения остаться на бесплатной модели пользователь прямо потребовал: прогнать
помощника по всем возможным сценариям, потому что ошибок может быть много; все ключевые
запросы должны отрабатывать; должен быть перевод на человека; плюс отдельно перечислил баги
самого виджета (дёрганая анимация открытия, скролл, надпись в шапке, горячие команды,
неполный список проверок сайта).

**Корневая архитектурная проблема, из-за которой лечение промпта не помогло бы:** в
`prepareSiteChange()` (`platform-assistant.js`) порядок был dialog → explicit-edit → OpenAI →
Cloudflare AI → локальный `localConversation()` → generic fallback. Раз Cloudflare AI почти
всегда возвращает *успешный*, но бессмысленный ответ (не `null`), локальный слой с готовыми
ответами физически никогда не достигался — даже отличный локальный классификатор ничего бы
не изменил на старом месте в цепочке. Исправлено: добавлен новый слой
`localAssistantAnswer(prompt, siteContext)`, который теперь стоит **до** любого обращения к AI
(и OpenAI, и Cloudflare) — детерминированные, проверенные ответы на известные категории вопросов
больше не зависят от качества бесплатной модели вообще.

Новый локальный классификатор покрывает (все регулярки собраны в широкие списки фраз, не одна
жёсткая формулировка):
- **Возможности** («что ты умеешь», «чем можешь помочь», «кто ты», «расскажи о себе» и т.д.) —
  фиксированный точный ответ о реальных функциях.
- **Статус сайта** («как дела», «сайт работает?», «статус сайта») — тот же расчёт, что и у
  кнопки «Проверь состояние сайта», но по свободной формулировке.
- **SEO/технические замечания** («какие проблемы», «что не так», «seo диагностика») — строится
  из `siteContext.diagnostics.issues`, с фильтром по категории, если явно упомянуто «seo».
- **Заявки/лиды** («заявки не приходят», «лиды») — тот же расчёт, что у кнопки
  «Почему могут не приходить заявки?».
- **Перевод на человека** («позови оператора», «нужен специалист», «переведи на поддержку»,
  «живой человек» и т.д.) — включает существующий UI-механизм `supportSuggested` (полоска
  «Позвать специалиста?» с кнопкой «Связаться с поддержкой»), которым раньше управляла только
  AI/чужой JSON-ответ, теперь доступен и локально, без зависимости от AI.

**Найден и исправлен системный баг в существующих regex**: `\b` (граница слова) в JS
**не распознаёт кириллицу как символ слова** даже с флагом `u` — подтверждено напрямую в Node
(`/ты\b/u.test('кто ты')` → `false`). Из-за этого несколько паттернов, включая уже существовавшую
(добавленную раньше) проверку приветствия `/^(?:привет|...)\b/iu`, никогда не срабатывали на
короткие сообщения без хвоста («привет» без продолжения не ловилось вообще). Все `\b` рядом
с кириллицей убраны/заменены на `(?:\s|[,.!?]|$)`; `\b` оставлен только там, где слово
латиницей (`seo`, `human`) — там он работает корректно из-за пробелов по краям. Проверено
отдельным Node-скриптом на ~30 формулировках до и после фикса.

`askAi()` (резервная бесплатная Cloudflare-модель) теперь тоже получает `siteContext`
(`currentStatus`, `diagnostics.summary`), а не только `inventory` — раньше эти данные уходили
только в OpenAI-путь. Это не решает проблему качества бесплатной модели полностью, но даёт ей
хотя бы реальные факты о сайте для вопросов, не попавших ни в один локальный паттерн.

**Баг с дёрганой анимацией открытия виджета** — найден не эмпирически (эта тестовая среда не
рендерит CSS-переходы и не всегда прогоняет `requestAnimationFrame` для фонового таба — оба
факта подтверждены отдельными пробами), а через анализ самой кривой: `cubic-bezier(.34,1.4,.64,1)`
имеет контрольную точку `y=1.4` — математически это гарантированный овершут (пружина), а
`opacity` завершался за 0.22s, тогда как `transform` ещё «пружинил» до 0.32s — то есть контент
становился полностью непрозрачным, пока ещё продолжал видимо скакать в размере. Заменено на
`cubic-bezier(.22,1,.36,1)` (плавное затухание без овершута) с одинаковой длительностью 0.26s
для обоих свойств; стартовый `scale(.3)` (сильный скачок) смягчён до `scale(.92)`.

**Баг с восстановлением позиции скролла при неродственных ре-рендерах** — код (`wasNearBottom`/
`grew`/`prevScrollTop`, добавленный ещё в коммите `3d8ba15`) оказался логически верным, но
опирался на двойной `requestAnimationFrame`, который в этой тестовой среде не выполняется вовсе
(подтверждено: `console.debug` внутри callback-а ни разу не сработал за несколько прогонов).
Заменено на синхронное присвоение `scrollTop` сразу после `innerHTML=...` — чтение
`scrollHeight` уже само по себе форсирует нужный layout, поэтому двойной rAF был избыточной
подстраховкой. Проверено вживую: ре-рендер без нового сообщения сохраняет позицию скролла
(50px как было), ре-рендер с новым сообщением уводит к низу — оба случая численно подтверждены.

**Надпись в шапке виджета** — оказалась не отдельным «тупым» текстом, а склейкой: серверный
`state.assistant.label` уже содержит «AI-помощник подключён», а клиентский код добавлял к нему
ещё `' на связи'` — получалось «AI-помощник подключён на связи», отсюда и ощущение неуклюжей
фразы. Склейка убрана, используется label как есть (fallback — «Онлайн»).

**Горячие команды в развёрнутом виде чата**: добавлены 2 новые кнопки — «Что ты умеешь?» и
«Позови специалиста поддержки» — видны только при `chatWidgetExpanded`, поверх обычных 4.
По прямому требованию пользователя («если могут сразу срабатывать, как быстрые команды») обе
сделаны через `data-editor-action="quick-*"` (мгновенный вызов `/assistant` и вставка ответа),
а не через `data-prompt-example` (просто вставка текста в поле, требующая ручной отправки) —
это тот самый паттерн, на который пользователь жаловался в прошлом раунде.

**Проверки сайта — не только SEO**: фактически уже была почти дюжина категорий
(`seo`/`content`/`accessibility`/`mobile`/`security`/`performance`/`availability`), просто
клиентский интерфейс их никак не показывал общим списком — отсюда жалоба «не знаю, какие ещё
проверки». Добавлены 3 новые проверки (Open Graph og:title/og:image под новой категорией
`social`, favicon, meta charset) и, главное, на странице «Диагностика» теперь всегда видна
строка из 7 категорий-чипов с количеством найденных замечаний или галочкой при их отсутствии —
проверено вживую («SEO: 2 · Контент ✓ · Доступность: 1 · Мобильная версия ✓ · Безопасность ✓ ·
Скорость ✓ · Соцсети: 1»).

Всё проверено вживую на локальном моке: свободный текст через реальное поле ввода на 5 разных
категорий вопросов подряд (статус/seo/лиды/human/возможности) — конверсация накапливается
корректно, ни разу не повторился шаблонный ответ; после серии сообщений нет горизонтального
скролла, вертикальный скролл работает и корректно стоит внизу. Полный набор тестов: 139/139
(2 теста обновлены — их проверяли на конкретный текст старого ответа/на факт похода в OpenAI,
оба сценария осмысленно изменились в результате этого рефакторинга, не регрессия).

Сознательно не сделано: сам классификатор для вопросов, не попавших ни в один локальный паттерн,
по-прежнему уходит в бесплатную Cloudflare-модель и может путаться — это фундаментальное
ограничение бесплатного пути, снять его может либо оплата OpenAI, либо дальнейшее расширение
локальных паттернов по мере поступления новых жалоб. Общая визуальная модернизация интерфейса
(«выглядит как в 2010 году») по-прежнему не начата — не запрашивалась в этом раунде.

## Три платных функции + доводка чата (2026-08-17)

Клиент прямо сказал: текущий платный функционал (SEO-анализ + правка телефона/графика/кнопки)
не ощущается как то, за что стоит платить — «SEO-анализ выполнит любая дешёвая нейросеть, а
замену блоков и так может сделать через Тильду любой дурак». Обсуждали накрутить более умную
платную модель — отклонено: модель улучшает только формулировки ответа, а не создаёт то, что
нельзя скопировать за вечер. Договорились вместо этого о трёх функциях, которые опираются на
данные, которые уже собирает SiteCare, и которых нет ни у дешёвых SEO-чекеров, ни у самой Tilda.

Одновременно клиент прислал скриншоты с конкретными багами виджета (пустой горизонтальный
скролл внизу чата, ответы одним сплошным абзацем вместо списка) — их сделали первым делом и
задеплоили отдельным раундом, не дожидаясь трёх больших фич.

### Доводка чата
- **Пустой горизонтальный скролл** — та же CSS-ловушка, что чинили раньше на `.assistant-chat`:
  `.chat-suggestions` задавал только `overflow-x:auto` без `overflow-y`, браузер сам
  доопределял вторую ось в `auto`. Добавили `overflow-y:hidden`. Это вскрыло реальный баг под
  скрытым скроллом: ни `.chat-suggestions`, ни `.chat-composer` не имели `flex-shrink:0`, и при
  выросшем (из-за нового списочного ответа) сообщении их могло сжать ниже высоты содержимого —
  кнопки подсказок обрезались. Добавили `flex-shrink:0` обоим, проверено вживую: `clientHeight`
  контейнера кнопок теперь равен `scrollHeight`, кнопки полной высоты (31px).
- **Ответы списком с «продающими» формулировками** — `capabilityAnswer`/`siteStatusAnswer`/
  `diagnosticsAnswer`/`leadsAnswer` в `platform-assistant.js` переписаны с слитных предложений
  через запятую/точку-с-запятой на список по строкам (`• `). Технически это потребовало
  разобраться в двухслойном экранировании: клиентский скрипт целиком лежит внутри серверной
  строки в `platform-ui.js`, поэтому `\n`, написанный напрямую в исходнике этой строки,
  обрабатывается парсером Node ещё на сервере и превращается в реальный перенос строки внутри
  regex-литерала клиентского кода — синтаксическая ошибка «Invalid regular expression: missing
  /» в браузере. Правильный вариант — `\\n` в исходнике (уже был такой в `messageText()`,
  сначала по ошибке «исправил» на `\n`, тесты (`central dashboard contains syntactically valid
  browser JavaScript`) сразу поймали и вернул обратно). Сообщения от AI-классификатора строятся
  как обычные JS-строки в отдельном серверном модуле (`platform-assistant.js`), поэтому там `\n`
  — это просто обычный перенос строки, без этой ловушки; сообщение уходит клиенту через
  JSON (который сам всё правильно экранирует), а не встраивается в код напрямую.
- Заодно нашли и починили баг: `capabilityAnswer()` жёстко возвращал 4 стандартные кнопки в
  поле `suggestions`, из-за чего после вопроса «что ты умеешь» переставали появляться 2
  дополнительные кнопки развёрнутого режима («Позови специалиста поддержки» и сам вопрос «Что
  ты умеешь?»). Теперь `suggestions` не задаётся явно, и включается стандартная логика
  клиента (4 в свёрнутом виде / 6 в развёрнутом).

### Функция 1 — Журнал изменений сайта
Детектирует правки, сделанные прямо в Tilda, а не через SiteCare, сравнивая ежедневные сканы
между собой. Технически самое сложное место — по чему сравнивать: существующие идентификаторы
кандидатов (`candidateId` у кнопок, `originalDigits` у телефонов) включают само содержимое в
идентификатор, поэтому при реальном изменении текста/номера «старое» и «новое» выглядят как два
независимых объекта, а не одно изменение. Решение — `slotKey = blockId|matchIndex`
(ID Tilda-блока + порядковый номер на странице), не зависящий от содержимого. Отслеживаются 7
полей: заголовок, description, H1, график, телефон, текст и ссылка кнопки. Новая ежедневная
джоба `runDueContentAudits` (по образцу уже существующей `runDueHealthScans`), новые таблицы
`platform_content_snapshots` (последнее известное значение) и `platform_content_changes`
(журнал, максимум 200 записей на сайт). Новый эндпоинт `GET .../content-changes`, новая карточка
на странице «Диагностика».

### Функция 2 — Юридическая проверка (152-ФЗ)
Три новые эвристические проверки в `diagnosePage()`: ссылка на политику конфиденциальности,
согласие на обработку персональных данных рядом с формой, баннер cookie. Честно предупреждаем в
интерфейсе: «автоматическая проверка HTML — не заменяет юридический аудит», поскольку баннер,
подключённый сторонним скриптом, в статичном HTML не виден. Отдельно решили важный нюанс:
ссылка на политику обычно живёт один раз в подвале сайта, а не на каждой странице — если судить
по каждой странице отдельно, получится по одному одинаковому замечанию на каждую страницу сайта.
Агрегируется на уровне всего сайта (по аналогии с уже существующим `duplicateFacts()` для
дублей title/description) — одно замечание, если ссылки нет **нигде**, а не N замечаний.
Отдельная карточка-чеклист на странице «Диагностика», плюс новая категория `legal` в общей
строке чипов категорий.

### Функция 3 — Домен/SSL и SLA по доступности
SSL сертификат — сознательно не делали: `fetch()` в Workers не отдаёт метаданные сертификата,
без костыля никак. Домен — через RDAP (`rdap.org`, бесплатно, без API-ключа, сам находит нужный
реестр по домену), новая еженедельная джоба `runDueDomainChecks`, новые поля на `platform_sites`
(`domain_expires_at` и т.д.). Telegram-уведомление за 30 дней до истечения — через уже
существующий `sendNotification()`, без отдельной таблицы «уже уведомляли»: `eventId` строится
как `site_id:domain-expiring:дата`, и `sendNotification` сам не пришлёт дубликат по одному и тому
же `eventId`.

Заодно нашли и попутно починили реальный баг, не связанный с этой задачей напрямую, но
блокирующий тот же `sendNotification()`, которым планировали воспользоваться: у таблицы
`gateway_deliveries` жёсткий `CHECK` на `event_type`, разрешающий только 6 старых значений,
но `sendDigest()` уже давно (с момента появления дайджестов) вызывает
`sendNotification(..., "digest", ...)` — то есть **еженедельный дайджест в Telegram все это время
падал молча** на каждой отправке (сам INSERT — вне try/catch, который ловит только сбои
Telegram). Ни один тест этот путь не проверял. Починили через пересоздание таблицы (в SQLite
нет `ALTER ... ALTER COLUMN` для CHECK) с расширенным списком: добавили `digest` и
`domain-expiring`.

Для аптайма — тоже нашли реальный, тихий баг: `platform_monitor_runs` хранит только последние
1000 строк на сайт, а при опросе раз в 5 минут это ~3.5 дня. `siteReport(env, siteId, days=30)`
при этом молча недосчитывал всё, что старше ~3.5 дней, хотя запрашивали до 90 дней. Добавили
ежедневный рулап в новую таблицу `platform_monitor_daily` (без ограничения по сроку хранения,
джоба `runDueMonitorRollups`) и переписали `siteReport()`: берёт рулап за весь период + сырые
данные только за сегодня (ещё не попавшие в рулап) — числа теперь точные на любом окне. Новая
карточка «Отчёт за месяц» первой на странице «Диагностика» — прямо то, что было нужно клиенту
(«чтобы клиент понимал, за что он заплатил»): доступность в %, средняя скорость ответа,
количество инцидентов, обратный отсчёт до истечения домена.

### Технический долг, обнаруженный по пути
Список миграций для Miniflare-тестов в `test/platform.test.js` — захардкожен вручную
(`PLATFORM_MIGRATIONS`), а не читается из папки автоматически. Уже был на 1 файл отстающим
(`0017_pending_selection.sql` пропущен в предыдущем раунде, просто ни один тест до сих пор не
трогал эту конкретную таблицу) — добавлены все 4 недостающих файла (0017–0020). Стоит когда-нибудь
переделать на автоматическое чтение директории, чтобы это не повторялось при каждой новой миграции.

Проверено: 143/143 теста зелёных (добавлено 4 новых юнит-теста на `buildContentFields`,
`checkDomainExpiry` и обе ветки юридических проверок), все три новые карточки проверены вживую
на локальном моке с реалистичными данными (просрочка домена, найденные юридические замечания,
записи журнала изменений).

Сознательно не сделано: настоящие LLM-агенты на каждую функцию — обсуждали, но это
противоречило бы решению не платить за AI (больше агентов = больше AI-запросов = тот же вопрос
с балансом OpenAI). Отслеживание позиций в поиске/конкурентов — самый «вау»-эффектный вариант
из обсуждавшихся, но требует платного API (тот же класс проблемы, что и с OpenAI) — не начато.

## Донастройка после первого показа новых карточек (2026-08-17, вечер)

Клиент прислал скриншоты сразу после деплоя трёх новых функций и нашёл 4 реальные проблемы.

**Кнопки быстрых действий в чате всё ещё выводили сплошной текст.** Оказалось, что списочный
формат ответов чинили только на сервере (`platform-assistant.js`, локальный классификатор
свободного текста) — а у кнопок «Проведи SEO-диагностику»/«Что можно улучшить?»/«Проверь
состояние сайта»/«Почему не приходят заявки?» есть СВОЯ, полностью независимая копия той же
логики на клиенте (`handleQuickAction` в `platform-ui.js`), которая ходит напрямую в `/check` и
`/inventory`, а не через `/assistant`. Про эту копию просто забыли при переписывании на списки в
прошлый раз. Переписана таким же образом (буллеты, вводная фраза, единый счётный формат
«замечание/замечания/замечаний»). Технически важный нюанс: чтобы клиентский JS-код содержал
настоящий escape-символ `\n` (который браузер сам превращает в перенос строки внутри строкового
литерала), в СЕРВЕРНОМ исходнике нужно писать `\\n` (двойной слэш) — то же самое двухслойное
экранирование, что уже ловили на функции `messageText()` в этом же раунде работы. Проверено:
`new Function(script)` над извлечённым клиентским скриптом не падает, а строка содержит ровно
двухсимвольный `\n`, а не реальный перенос.

**Диагностика на странице запускалась сама по себе от действий в чате.** Причина —
`editorInventory` (переменная, которая определяет, показывать ли на странице «Диагностика»
результаты сканирования) была ОБЩАЯ между самой страницей и чатом: при ЛЮБОМ сообщении в чат
(`prepareEditorChange`), при кнопках «Проведи SEO-диагностику»/«Что можно улучшить?»
(`handleQuickAction`) и при клике по номеру на сайте (`applyLocatedPhone`) в неё попадали
данные сканирования сайта, которые сервер всё равно возвращает в ответе `/assistant` независимо
от содержания вопроса (нужны для сопоставления кандидатов на телефон/кнопку). Клиент был прав:
раз диагностика — это отдельная, требующая явного запуска функция, её состояние не должно
меняться как побочный эффект от чата. Ввели отдельную переменную `chatObserved` только для
данных, которые нужны самому чату (текущее значение телефона/графика для карточки «было →
станет»), и убрали все мутации `editorInventory` из чатовых путей — теперь она меняется
единственным легитимным способом, кнопкой «Запустить/Обновить диагностику» на самой странице.
Проверено вживую: после вопроса «Что ты умеешь?» в чате страница диагностики по-прежнему
показывает «Нажмите «Запустить диагностику»», хотя чат отвечает корректно.

**Неровные по ширине плитки в «Отчёт за месяц».** Не удалось воспроизвести вживую: проверял сетку
`.grid.metrics` (`grid-template-columns:repeat(4,minmax(0,1fr))`, стандартный паттерн равных
колонок) на пяти разных ширинах вьюпорта от 600 до 1280px — во всех случаях плитки получались
математически равны по ширине (проверено через `getBoundingClientRect()`). На всякий случай
добавил `min-width:0` на `.metric` — стандартная защита grid/flex-элементов от того, чтобы
неразрывный длинный текст раздувал свою колонку шире соседних. Если проблема не пропадёт после
этого деплоя — нужен снимок с точной шириной окна браузера, чтобы поймать точный кейс.

**Непонятные метрики без пояснений.** Добавлены `title`-тултипы (всплывают при наведении) на все
4 плитки: «Доступность сайта» (что за проценты и что значит меньше 100%), «Средняя скорость
ответа» (что означает и какие цифры хорошие/плохие), «Инциденты», «Домен» — простыми словами,
без техножаргона.

**Повторная проверка «сначала расти, потом скроллить».** Клиент напомнил про это требование
отдельным сообщением, похоже, до того как увидел уже задеплоенный фикс. Перепроверил вживую с
НОВЫМИ более длинными (списочными) сообщениями как раз тем сценарием, который мог сломать
поведение: пустой чат — 507px, без скролла; после одного обмена репликами (уже длинного,
списочного) — 600px (потолок) и скролл включается. Механизм работает верно и на новом,
более требовательном тесте. Артефакт на скриншоте — маленький «хвостик» скруглённого угла у
поля ввода — не смог воспроизвести и найти структурную причину (composer не выходит за границы
родителя, `overflow:hidden` у виджета стоит); нужен более точный скриншот/шаги для повторной
попытки, если он не исчез после этого раунда фиксов.

Полный набор тестов остаётся 143/143 после всех правок этого раунда.

## Две точечные правки следом (2026-08-17, тем же вечером)

**Карточка «Отчёт за месяц» уже шире «Состояния сайта».** Клиент спросил напрямую: «ты видишь,
что у них разная ширина?» — и оказался прав, хотя это была не та проблема, что я перепроверял
раньше (тогда смотрел на плитки ВНУТРИ одной сетки метрик, все были равны). Реальная причина —
`monthlyReportSection` был вставлен ВНЕ `<div class="editor-stack diagnostics-only">`, у которого
есть `max-width:980px`; карточка «Отчёт за месяц» росла на всю доступную ширину страницы, а
«Состояние сайта» под ней — ограничена 980px. Перенёс `monthlyReportSection` внутрь того же
контейнера. Проверено на 1440px: все три карточки («Отчёт за месяц», «Состояние сайта», «Журнал
изменений сайта») теперь ровно по 980px и с одинаковым отступом слева.

**Ассистент сам предлагал «спросите», а потом не мог ответить.** После списка SEO-замечаний
сообщение заканчивается фразой «Могу разобрать любой пункт подробнее — просто спросите». Клиент
показал скриншотами: спросил «какая длина заголовка должна быть?» — получил дежурное «Уточните,
пожалуйста, что нужно изменить.» дважды подряд. Причина та же, что и раньше: вопрос не совпал ни
с одним локальным паттерном и ушёл в слабую бесплатную модель. Но в этом случае это прямой удар
по доверию — ассистент сам предложил спросить и не смог ответить на самый очевидный вопрос про
только что показанную находку. Добавлена локальная база из 12 тем (что проверяет `diagnosePage()`:
длина title/description, H1, alt, смешанный контент, canonical, og-теги, favicon, кодировка,
скорость, 152-ФЗ, cookie) со статичными, точными ответами — не через AI, поэтому работают всегда
независимо от баланса OpenAI. При первой версии паттернов промахнулся на порядке слов («сколько
символов в описании» — «символов» раньше «описании» в тексте, а регулярка требовала обратный
порядок) — исправил на двусторонние паттерны. Проверено на всех 8 тестовых формулировках и живым
сценарием «нажал кнопку SEO-диагностики → задал именно тот вопрос, что на скриншоте клиента» —
теперь отвечает по существу.

## Оптимистичный UI отправки + разбор «пропавшего» сообщения (2026-08-17, ночь)

**«Моё сообщение должно сразу быть в чате, а не пытаться отправиться снизу».** До этого раунда
`prepareEditorChange()` ждал полного ответа сервера, прежде чем вообще показать сообщение
клиента — во время ожидания текстовое поле просто становилось disabled с уже введённым текстом
внутри, что выглядело как «зависло». Переписал на оптимистичное обновление: при нажатии
«Отправить» сообщение клиента сразу добавляется в локальный `s._conversation.messages`, поле
очищается и блокируется, показывается индикатор «печатает» — всё это одним синхронным
`render()` до начала сетевого запроса. Когда сервер отвечает — локальное состояние заменяется на
серверное (с ответом AI). Если запрос падает — оптимистичное сообщение аккуратно откатывается
из списка, а введённый текст возвращается в поле, чтобы не потерять черновик. Добавлен новый
флаг `chatSending`, который отдельно от `chatEnabled` (доступность сайта) блокирует поле/кнопку
на время отправки — раньше поле блокировалось вручную на конкретном DOM-узле, что несовместимо
с оптимистичным подходом, где `render()` теперь вызывается ДО ответа сервера и пересоздаёт узел.
Проверено вживую с искусственной задержкой ответа: в момент t=0 (сразу после клика) сообщение
уже видно, поле пустое и заблокировано, индикатор «печатает» показан; после ответа — восстанавливается
корректно. Отдельно проверен путь ошибки (эмулировал сетевой сбой) — сообщение убирается,
текст восстанавливается в поле, поле разблокируется.

**«Почему бот удаляет своё предыдущее сообщение после моего?»** Перед тем как чинить, проверил
это как реальную гипотезу data-loss — прошёлся по `ensureConversation()`/`conversationSnapshot()`/
`appendConversationMessage()` в `platform-support.js`: логика поиска существующего диалога по
(site_id, user_id) корректна, лимит истории (300 сообщений на диалог, отдаётся до 150 за раз) —
с большим запасом для реального диалога. Прямых доказательств потери данных на сервере не нашёл.
Проверил вживую другую гипотезу — что сообщение никуда не делось, а просто scroll ушёл вниз к
новому ответу: после 4 сообщений в переписке `totalMessagesInDom: 4` (все на месте), но
`scrollHeight: 873` при видимой высоте `clientHeight: 386` — то есть видна едва ли половина
истории, и виджет корректно проскроллен вниз (`isScrolledToBottom: true`), а самое старое
сообщение просто выше видимой области (`firstMessageVisible: false`). Иначе говоря: ничего не
удаляется, просто ответы стали длиннее (списки вместо одной строки) и в компактном окне чата
теперь физически помещается меньше истории одновременно — так же ведёт себя любой мессенджер
(включая тот, которым клиент пользовался как образец поведения чата в одном из более ранних
сообщений). Изменений по этому пункту не вносил — это не баг, это следствие более длинных и
полезных ответов.

**Ещё 2 пробела в базе FAQ.** «Какое описание должно быть у изображения?» — та же ошибка
порядка слов, что чинил раньше для заголовка/description, только для темы alt-текста
(«изображения... описание» вместо ожидаемого «описание... изображения»). Вместо точечных
патчей каждого направления переписал все паттерны в базе на конструкции вида
`/(?=.*словоА)(?=.*словоБ)/` (положительный lookahead) — они матчат оба слова независимо от
порядка, тот же класс проблемы больше не повторится ни для одной темы. Заодно нашёл и добавил
недостающий короткий вариант для og-тегов («что за og теги», без двоеточия). Проверено на 14
формулировках, все проходят.

Тесты 143/143.

Тесты остаются 143/143.

## Настоящая причина «пропадающего» сообщения со списком найдена и исправлена (2026-08-17, ночь, продолжение)

Клиент поправил предыдущий вывод: сценарий из скринов — не свободный ввод текста, а нажатие
кнопки быстрого действия («Что можно улучшить?» → бот выдаёт список замечаний), после чего
следует уточняющий вопрос обычным текстом. Именно этот смешанный сценарий раньше не тестировался.

Причина: `injectQuickAnswer()` (реакция на кнопки быстрых действий: quick-check/quick-seo/
quick-improve/quick-leads) писала пару сообщений (вопрос+ответ) только в локальный
`s._conversation.messages` и вызывала `render()` — на сервер ничего не уходило. Как только
клиент затем отправлял обычное сообщение через `prepareEditorChange()`, ответ сервера **целиком
заменял** `s._conversation` своим снимком из D1 (`selected._conversation=result.conversation`),
в котором пары от кнопки никогда не было. Внешне это выглядело как «бот стирает своё сообщение
со списком» — на самом деле оно так и не сохранялось на сервере, а следующий же серверный
ответ полностью перетирал локальное состояние авторитетным снимком из базы.

Исправление:
- Новый эндпоинт `POST /v1/platform/sites/:id/conversation/quick` (`platform.js`,
  `appendQuickExchange()`) — пишет пару client+ai сообщений через `appendConversationMessage()`
  (те же функции, что использует обычный путь через AI-помощника) и возвращает свежий
  `conversationSnapshot()`. Лимит `enforceActionLimit` 30/300с, как у соседних экшенов.
- `injectQuickAnswer()` (`platform-ui.js`) стала `async`: сразу показывает пару сообщений
  локально (оптимистично, как обычная отправка), затем вызывает новый эндпоинт и заменяет
  `s._conversation` на серверный снимок. Если запрос упал — сообщения остаются локально видны
  (тихий catch), следующий обычный обмен просто пересинхронизирует состояние.

Проверено вживую на мок-сервере с общим объектом «базы» между `/conversation/quick` и
`/assistant` (важно — раньше мок-серверы этого раунда сессии использовали независимую логику
на каждый эндпоинт, из-за чего этот класс бага было невозможно поймать). Сценарий: клик
quick-improve → пауза 500мс на сохранение → отправка обычного сообщения «что такое h1» →
пауза 500мс → проверка `document.querySelectorAll('.chat-message')`. Результат: все 4 сообщения
на месте (`afterQuick:2, afterFollowup:4`), пара от кнопки пережила последующий обмен.

Тесты 143/143.

## Модуль «Отзывы»: честные состояния оплачено/не оплачено + первый заход дизайна (2026-08-17, продолжение)

**Клиент указал на нелогичный UX**: карточка «Отзывы» в оплаченном, но ещё не настроенном
состоянии показывала кнопку «Запросить подключение источников», которая на самом деле ничего
не запрашивала — просто открывала информационный диалог с инструкцией. Непонятно было и что
происходит после нажатия. Переписал `renderClientReviews()` (`platform-ui.js`) на 4 честных
состояния вместо 2:
1. **Не оплачено** — как раньше, `lockedModule()` (карточка с питчем и кнопкой покупки), пункт
   в боковом меню остаётся видимым с замком (не скрываем — это единственный способ узнать о
   существовании функции и захотеть её купить).
2. **Оплачено, источники не настроены, заявка ещё не отправлена** — кнопка «Написать в
   поддержку» теперь по-настоящему создаёт обращение через существующий эндпоинт
   `POST /sites/:id/support` (тот же, что использует ИИ-помощник для передачи задачи
   специалисту) — не просто открывает инструкцию.
3. **Оплачено, заявка уже передана поддержке** (новое состояние, определяется по
   `s._conversation.supportRequest`, лениво подгружается тем же паттерном, что и для
   плавающего чата) — статус «Уже занимаемся подключением» с датой заявки, без повторной
   кнопки — ничего лишний раз нажимать не нужно.
4. **Оплачено, источники настроены** — как раньше, показывается сам виджет (iframe).

Проверено вживую на мок-сервере всеми 4 переключением сценариев: полный текст каждого
состояния, включая реальный переход 2→3 после клика по кнопке (заявка правда появляется через
`/support`, счётчик `created` идемпотентен при повторном клике).

**Мелкий шрифт и более современный вид (общая просьба клиента).** Аудит CSS показал 111
использований `font-size` в диапазоне 9–13px — почти все это подписи, вторичный текст в
карточках (`.list-row p`, `.history-item p`, `.card-head p`, `.setup-note`, `.event time` и
т.д.), то есть именно то, на что жаловался клиент по скриншоту «Последние изменения». Поднял
каждое значение в этом диапазоне на 1px (`9→10, 10→11, 11→12, 12→13, 13→14`) скриптом по
всему `<style>`-блоку — точечно только маленький текст, крупные заголовки и иконки-глифы
(21px+) не трогал, чтобы не сломать уже выверенные пропорции макета.

**Анимации и «более современный» интерфейс.** Добавил:
- Лёгкий hover-подъём карточек (`.card:hover,.status-tile:hover{box-shadow:...;transform:
  translateY(-2px)}`) с transition на `.metric,.card,.status-tile`.
- Тактильный отклик кнопок: `transform:scale(.97)` на `:active` + плавный transition вместо
  мгновенного `filter:brightness()`.
- Fade+slide анимация появления страницы (`.view-enter`, keyframes `viewEnter`) при **реальной
  навигации** между разделами.

Последний пункт — самый рискованный: `render()` вызывается очень часто (каждый тик
оптимистичного UI чата, каждые 9 секунд опрос поддержки), и если триггерить анимацию на каждый
вызов `render()`, получится ровно тот баг с дёрганьем, что уже чинили в этой сессии (коммит
`3d8ba15`). Поэтому анимация переигрывается только когда `currentView` реально меняется —
через module-level `lastAnimatedView` и forced-reflow-трюк (`classList.remove`→
`void view.offsetWidth`→`classList.add`, без `requestAnimationFrame`, который в этом
браузерном инструменте ненадёжен для фоновых вкладок). Добавлен `@media(prefers-reduced-
motion:reduce)` — отключает все новые transition/animation.

Проверено вживую: патчил `classList.add/remove` на `#view`, чтобы посчитать реальные вызовы —
переход между разделами даёт ровно 1 add+1 remove, отправка сообщения в чат (открытие бабла,
optimistic-рендер, серверный ответ) — 0. Прямое измерение через CSS Animations API
(`animationstart`/`getAnimations()`) оказалось ненадёжным в этом инструменте — `currentTime`
у аниме замирал на 0 даже для валидных срабатываний, похоже на уже известную проблему с rAF/
таймерами для неактивных вкладок в этой среде, поэтому не использовал его как источник истины.

Задача про перенос стрелки сворачивания бокового меню (следующая по списку) — ещё не начата.

Тесты 143/143.

## Настоящая причина «сплошного текста» найдена: сервер сам стирал переносы строк (2026-08-17, ночь)

Клиент снова показал скриншот, где ответ помощника («Вот что я умею…») пришёл одной сплошной
простынёй текста вместо списка с буллетами, и спросил, можно ли проверить это системно, а не
точечно. Раньше в этой сессии такие баги чинили на клиенте (`messageText()`/двойное
экранирование `\n` в `platform-ui.js`) — но в этот раз клиентский код был ни при чём: проверил
байты самого регэкспа прямо на проде (`fetch(location.href)` + подсчёт символов `\`) — там
корректный `/\n/gu`, ровно один бэкслеш, всё как надо.

**Настоящая причина — на сервере.** `capabilityAnswer()` (и вообще любой ответ помощника)
собирает текст через `lines.join("\n")` с настоящими переносами. Но при сохранении в БД
`appendConversationMessage()` (`platform-support.js`) прогоняет контент через
`normalizedContent()` → `safeText()` (`platform-core.js`), а у `safeText` в третьей строке
`.replace(/\s+/gu," ")` — схлопывает ЛЮБОЙ пробельный символ, включая `\n`, в один пробел. То
есть каждое сообщение с буллетами превращалось в один абзац **в момент записи в D1**, независимо
от того, как аккуратно оно было собрано выше по стеку. Поэтому все точечные правки раньше в
этой сессии (переписать `capabilityAnswer`, `diagnosticsAnswer`, бул-листы в quick-action
кнопках) работали только в момент оптимистичного локального рендера — как только приходил
ответ сервера (а он приходит всегда, для quick-action тоже с прошлого раунда), список
схлопывался обратно в кашу.

Нашёл этот же паттерн трижды:
1. `platform-support.js` → `normalizedContent()` — общая точка для **всех** сообщений диалога
   (главная причина, чинит сразу все ответы помощника, локальные и AI).
2. `platform.js` → `appendQuickExchange()` (код из прошлого раунда этой же сессии) — там
   `aiText` дополнительно прогонялся через `safeText()` ДО вызова `appendConversationMessage`,
   то есть дублировал схлопывание ещё раз, до общей точки.
3. `platform-openai.js` → `parseAssistantResponse()` — ответ самой модели (`parsed.reply`) тоже
   шёл через `safeText`, значит и настоящие ответы OpenAI (не только локальный классификатор)
   теряли бы форматирование, если бы модель вернула список.

**Исправление**: добавил `safeMessageText()` в `platform-core.js` — те же гарантии безопасности,
что у `safeText` (вырезает управляющие символы и `<>`, ограничивает длину), но схлопывает только
пробелы/табы внутри строки (`[^\S\n]+`), сохраняя одиночные `\n`, нормализует `\r\n`/`\r` → `\n`
и ограничивает разгон пустых строк (`\n{3,}` → `\n\n`). Заменил `safeText` на `safeMessageText`
во всех трёх точках выше — только там, где текст действительно многострочный контент чата;
остальные вызовы `safeText` (имена, короткие метки, `targetHint`, `supportSummary`) не трогал —
им однострочность и нужна.

Проверено юнит-тестом самой функции на реалистичных кейсах (буллеты, CRLF, разгон пустых строк,
обрезка длины) — переносы сохраняются, всё остальное схлопывается как раньше. Полный прогон
через настоящий D1 (`appendConversationMessage` → `conversationSnapshot` → рендер) локально не
поднять — Miniflare на этой Windows-машине не работает (см. пункт 1a в начале файла), эта
цепочка проверяется только через CI. Автономно проверил только сами функции — этого достаточно
для уверенности, так как баг был чисто в одной строке одного регэкспа, а не в передаче данных
между слоями.

Тесты 143/143.

## Визуальный рефреш 7.1: слияние на месте вместо ещё одного слоя (2026-08-17, ночь)

Клиент прислал `SiteCare-modern-ui.zip` — свою версию `platform-ui.js` с чисто визуальной правкой
(43 строки CSS, без единой правки JS-логики: обновлённая палитра, экран входа, сайдбар, карточки/
метрики, блок диагностики, лиды, чат-виджет, диалоги/тосты, три медиа-запроса). Diff показал ровно
один блок, добавленный в конец `<style>` перед `prefers-reduced-motion` — то есть ещё один
override-слой поверх каскада, как это уже делалось для версий 5.x/6.0/6.2/6.5/7.0/7.0.3 (в файле
это видно по комментариям вида `/* 6.2: ... */`).

Живьём проверил слой как есть (мок-сервер + реальный `platformHtml()`) — рендерится корректно на
десктопе и мобильной ширине, без переполнения, без ошибок консоли.

**Клиент прямо попросил не накладывать очередным слоем, а внести значения в существующие правила
на месте.** Предупредил про риск (файл — это ~130 очень длинных строк CSS, многие свойства уже
переопределены по 2-4 раза более ранними слоями; редактирование вручную без сборки/линтера может
что-то задеть) — клиент подтвердил, что всё равно хочет слияние на месте.

Сделано скриптом (`merge.mjs`, одноразовый, в скрэтче) с точными якорями и проверкой «ровно 1
совпадение» перед каждой заменой:
- `:root` (палитра), `body{background}`, кнопки `.primary/.secondary/.ghost`, `.toast` — обычные
  значения-в-одном-месте, отредактированы прямо в исходном правиле (это единственное место, где
  соответствующее свойство вообще задаётся).
- Крупные тематические блоки (логин, сайдбар/шапка, карточки/метрики, лиды, диагностика,
  чат-виджет) вставлены НЕ одним блоком в конец, а короткими вставками сразу после
  соответствующего тематического места в файле (например, правки логина — сразу после
  оригинального `.auth{...}`, правки сайдбара — после последнего слоя про wordmark и т.д.),
  с маленькими комментариями `/* 7.1: ... */` — так же, как это делали версии 6.0/6.2/6.5.
- Отдельно проверил порядок для `.chat-widget` на мобильной медиа-query: если вставить
  безусловное правило `.chat-widget{...}` ПОСЛЕ существующего `@media(max-width:620px)`, оно бы
  на мобильных перебивало мобильный override той же специфичности — вставил ДО него, а новые
  медиа-запросы (620/860/1120px) — уже после всех старых с теми же брейкпоинтами, чтобы корректно
  выигрывали финальные значения.

**Проверка эквивалентности**: не просто «выглядит нормально» — сравнил вычисленные стили (`getComputedStyle`)
для примерно 15 конкретных точек (переменные `:root` через фон кнопки, границы карточек, тень,
градиент `.primary`, высота `.metric`, радиусы `.auth-card`/`.diagnostics-cta`/`.compact-status`,
`max-width` `.diagnostics-only`, поведение чат-виджета и мобильного брейкпоинта) между версией
«слоем в конце» и версией «слито на месте» — все значения совпали побайтово. Это и есть настоящая
гарантия, что слияние прошло правильно, а не просто «на глаз».

Тесты 143/143 после слияния, `node --check` чисто.

## Полная переработка дизайна под макет клиента — Фаза A (2026-08-18)

Клиент прислал скриншот-макет дашборда (плитки статистики с цветными иконками, прогресс-бары по
категориям диагностики, страница «Главная», 8 пунктов меню) и попросил переделать интерфейс под
него «один в один» — не только палитру, но и расположение блоков. Согласовали через режим
планирования (`/plan`, файл сохранён как `adaptive-squishing-sparrow.md`) четыре фазы:

- **Фаза A (эта запись)** — только визуальная система: палитра сайдбара, профиль в шапке вместо
  подвала сайдбара, карточка апсейла «SiteCare Pro» на его месте, один новый значок меню (лупа
  вместо домика для «Диагностики»).
- Фаза B — новая страница «Главная» + формула балла для процентов диагностики (клиент выбрал
  «придумать честную формулу» вместо голых процентов без обоснования).
- Фаза C — разнесение контента: «Сайты» (список сайтов из Настроек + месячный отчёт из
  Диагностики), «Безопасность» (issues категории security + чек-лист 152-ФЗ, оба сейчас на
  Диагностике), «Подписка» (тарифы, сейчас в Настройках).
- Фаза D — «Помощь»: FAQ на основе уже существующей базы `EXPLAIN_TOPICS`.

**Важное решение по составу меню**: пункты макета не совпадают 1:1 с нашими реальными разделами
(«Безопасность» — просто категория внутри диагностики, «Сайты»/«Помощь» вообще не существуют).
Вместо пустых вкладок-заглушек — разнесение уже существующего контента с переполненных страниц
Диагностика/Настройки по новым, каждая вкладка сразу с реальным содержимым. «Отзывы» (платная
функция, которой нет на макете) остаётся в меню — не выкидываем функциональность ради шаблона.

**Сделано в Фазе A**:
- `.sidebar{background:...}` — убран фиолетовый радиальный градиент, сплошной тёмный
  `#12151f` (было `radial-gradient(...),linear-gradient(180deg,#111720 0%,#131923 100%)`).
- Активный пункт меню — сплошная фиолетовая плашка (`background:var(--brand)`) вместо
  градиента+inset-тени.
- Кнопка профиля перенесена из `.side-foot` (низ сайдбара) в `.workspace-head` (шапка справа) —
  тот же `id="profileButton"`, поэтому вся существующая JS-логика (`.onclick`, `load()` заполняет
  `profileName`/`profileEmail`/`profileAvatar`) продолжает работать без изменений, просто элемент
  теперь в другом месте DOM.
- На освободившееся место в сайдбаре — карточка «SiteCare Pro» (`#sidebarUpsell`), видимость
  переключается в `renderHeader()` (и в operator-preview варианте) по `hasFeature(a,'control')&&
  hasFeature(a,'reviews')` — скрыта, если оба продукта уже куплены, скрыта для сотрудников
  поддержки вне preview-режима.
- Значок «Диагностика» — лупа вместо домика (семантически ближе к макету).

Живьём проверено на мок-сервере с двумя аккаунтами (неполный/полный бандл) — карточка апсейла
корректно показывается/переключается, кнопка профиля кликабельна и попадает в шапку, значок-лупа
рендерится, оверфлоу нет. Тесты 143/143, `node --check` чисто.

## Полная переработка дизайна — Фаза B: страница «Главная» (2026-08-18)

Новая страница `client-home` — теперь стартовая (была «Диагностика»). Реализована по плану из
`adaptive-squishing-sparrow.md`, никаких новых эндпоинтов не понадобилось — вся правка на стороне
клиента плюс одно точечное расширение существующего ответа `/inventory`.

**Формула балла для процентов** (клиент выбрал это вместо голых цифр без обоснования). Вместо
изобретения новой формулы — переиспользовал уже существующую `computeHealthScore({high,medium,low})`
из `platform-monitor.js` (`100 - 15×high - 5×medium - 1×low`, уже используется для общего
`health_score` сайта, который считается раз в сутки). Новая функция
`categorySeverityCounts(issues, category)` просто фильтрует issues по категории и считает
high/medium/low — а дальше тот же `computeHealthScore`. То есть общий скор сайта и 4 балла по
категориям (SEO/Скорость/Мобильная/Безопасность) — теперь одна и та же формула, а не две разные
философии подсчёта. Посчитано в `scanSiteInventory()` и добавлено как `diagnostics.summary.
categoryScores` в существующий ответ `/inventory` — никакой новой миграции, никакого нового
эндпоинта. 2 новых юнит-теста (изоляция категории + сквозная проверка на `scanSiteInventory`).

**Что на странице**: 4 плитки статистики с цветными значками (Страниц/Ошибок/Предупреждений/Заявок
— первые три показывают «—» до первого запуска диагностики за сессию, «Заявок» доступно сразу, это
данные из заявок, а не из диагностики), карточка «Диагностика сайта» с 4 прогресс-барами (цвет
переключается ok/warn/bad по тем же порогам, что уже использует `.pill`), карточка «Запустить
диагностику» (переиспользует существующий обработчик `run-diagnostics`, только другая обёртка и
усиленная тень у кнопки — см. ниже), таблица «Последние заявки» (3 последних, «Все заявки →»
ведёт на вкладку Заявки).

**Контраст кнопки «Запустить диагностику».** Клиент отметил, что кнопка «сливается с блоком» на
существующей странице Диагностики — проверил вычисленные стили: `.primary` там уже непрозрачный
насыщенный фиолетовый на светло-лавандовом фоне карточки, контраст по цифрам в порядке, явного
бага не нашёл. Не стал трогать существующую кнопку без более точного репродукса, но на новой
кнопке Главной подстраховался — добавил `.home-cta .run-diagnostics{box-shadow:0 10px 24px
rgba(101,80,230,.32)}`, заметно более выраженная тень-подъём, чем у базового `.primary`. Дешёвая
подстраховка независимо от того, в чём была настоящая причина жалобы.

Живьём проверено: значения плиток и баллы гейджей на пустом состоянии (диагностика ещё не
запускалась в этой сессии) и после запуска (`Запустить диагностику` → реальные числа/цвета из
мок-данных, включая переключение gauge в `warn` при барале 85%), переход «Все заявки →» на вкладку
Заявки, мобильная ширина (плитки в один столбец), оверфлоу нет. Тесты 144/144.

## Фаза B, доводка после реального фидбека клиента (2026-08-18, вечер)

Клиент прислал два скриншота с реальными проблемами вёрстки — это застало врасплох, потому что
предыдущая проверка (вычисленные стили + геометрия) их не поймала. Разбор и фикс каждой:

**1. Кнопка «Запустить диагностику» наезжала на заголовок карточки.** Причина —
`.diagnostics-cta` в файле переопределялся трижды по слоям (5.x: `display:flex`, 7.0: **`display:
grid;grid-template-columns:52px minmax(0,1fr) auto`** — этот слой и оказался последним
побеждающим), а мой новый `.home-cta{flex-direction:column}` пытался переключить на flex-раскладку
свойствами, которые на grid-контейнере **ничего не делают** — контейнер как был grid, так и
остался, кнопка сидела в третьей auto-колонке и наезжала на текст, когда карточка стала уже
(на Главной она делит место с блоком гейджей в `.two-col`, а не на всю ширину, как на исходной
странице Диагностики). Почему не поймал раньше: проверял только цвета/текст/наличие элементов
через `getComputedStyle`, ни разу не проверял **геометрию на пересечение**. Фикс: `.home-cta`
теперь тоже `grid-template-columns:1fr` (тот же приём, что уже используют существующие
адаптивные версии этого блока под узкие экраны) + `.run-diagnostics{grid-column:1/-1}`. Проверено
на этот раз через `getBoundingClientRect()` и математическую проверку пересечения прямоугольников
всех дочерних элементов — пересечений нет.

**2. Плитки статистики «все одного белого цвета, иконки мелкие».** Значки были 34px с очень
бледными фоновыми оттенками (`--soft`, `--redbg` и т.д. — почти белые). Увеличил до 42px,
подобрал более насыщенные оттенки для фона значка (не трогая сами цвета текста/обводки — они
были в порядке), добавил тень на плитку при наведении.

**Дополнительные пожелания клиента, реализованы в этом же раунде:**
- Гейджи категорий стали кликабельными кнопками — открывают диалог с реальным списком замечаний
  по категории и объяснением формулы балла (`categoryDetailDialog()`, переиспользует существующий
  `showDialog()`). `categoryScores` расширен с 4 до всех 8 категорий диагностики; primary 4
  (SEO/Скорость/Мобильная/Безопасность) видны всегда, secondary 4 (Контент/Доступность/Соцсети/
  Правовые требования) — в раскрывающемся блоке «Все отчёты» с анимацией по max-height и
  поворотом шеврона.
- Профиль возвращён обратно в низ сайдбара (клиенту не понравилось в шапке) — карточка апсейла
  «SiteCare Pro» и профиль теперь вместе в `.side-foot`.
- В шапку вместо профиля добавлена новая кнопка «Изменения» — выпадающая панель с последними
  правками сайта (переиспользует `changeHistoryRows()`), анимация появления через opacity+
  translateY (не через `.hidden`, у которого `!important`, — своя схема с классом `.open`),
  закрытие по клику вне панели.

**Баг №2 при отладке анимации `.gauge-more` (max-height 0→420px не применялся):** потратил время
на разбор, оказалось не баг — тот же класс проблемы, что уже описан в этом файле для
`animationstart`/`getAnimations()`: CSS-переходы ненадёжно доигрывают в этом браузерном
инструменте для фонового/неактивного таба. Подтверждено контрольным тестом: с `transition:none`
класс `.expanded` применяет `max-height:420px` мгновенно и корректно; сама CSS-специфичность и
JS-логика переключения класса верны. Не источник истины для будущих анимаций в этом инструменте —
проверять через отключение transition, а не через ожидание завершения.

**Баг №3, найден тем же методом (пересечение прямоугольников):** выпадающая панель «Изменения»
не помещалась под шапкой (`top` считался от `.head-actions`, а это flex-элемент высотой 46px,
центрированный внутри шапки высотой 68px — на 9px короче, чем сама шапка). Перенёс
`position:relative` с `.head-actions` на `.workspace-head` целиком — теперь `top:calc(100% +
10px)` считается от полной высоты шапки.

Тесты 144/144, `node --check` чисто.

## Ещё раунд реальных багов после живой проверки клиентом (2026-08-18, ночь)

Клиент прислал ещё скриншоты — на этот раз уже прямо с прода, не с мока. Разбор каждого:

**1. Плитка «Домен: не удалось проверить» наезжала на выпадающую панель «Изменения».** Это
прямое следствие моего же фикса из предыдущего раунда: чтобы панель корректно позиционировалась
под шапкой, я добавил `.workspace-head{position:relative}` — но БЕЗ явного `z-index`. У плиток
`.metric` уже давно стоит `position:relative` (для декоративной полоски снизу через `:after`).
Когда у обоих элементов `position` задан, а `z-index` — нет (`auto`), порядок отрисовки решает
не вложенность, а **порядок в DOM**: `main` с плитками идёт в разметке ПОСЛЕ `header`, значит
рисуется поверх него — несмотря на то, что у самой панели `.changes-dropdown` стоит `z-index:40`.
Этот `z-index` работает только внутри стекингового контекста шапки, а не глобально. Фикс:
`.workspace-head{z-index:20}` — теперь у шапки явный номер, и она со всем содержимым (включая
панель) рисуется поверх всего, что не задаёт z-index сама. Проверено не через пересечение
прямоугольников (это как раз не показывает, кто сверху), а через `document.elementFromPoint()`
в точке пересечения — теперь возвращает элемент из панели, не из плитки.

**2. Кнопка «Запустить диагностику» — по факту это была не одна жалоба, а десять** (клиент
абсолютно справедливо это подчеркнул). Помимо бага из прошлого раунда с наездом на заголовок
(на Главной), была отдельная, более старая претензия — сам текст кнопки читается плохо на
исходной странице Диагностики. Причин точно установить в этой среде нельзя — здесь недоступны
скриншоты (`computer{screenshot}` стабильно падает с «Browser pane is not displayed» весь этот
сеанс), поэтому пиксельно проверить текст невозможно. Сделал две вещи, которые применимы
независимо от точной причины: убрал декоративную точку `:before` перед текстом (`content:none`) —
она давала мягкую белую тень 5px рядом с началом текста и была главным подозреваемым в «замыленности»;
и заметно усилил сам текст — `font-weight:800` (было 760), `font-size:15px`, чуть плотнее
`letter-spacing`. Это касается обоих мест, где используется кнопка (исходная Диагностика и Главная),
так как они делят один и тот же CSS-класс. Честно предупредил клиента, что не могу подтвердить
результат «на глаз» в этой среде — нужно его подтверждение после деплоя.

**3. Стрелка сворачивания сайдбара** (задача, висевшая с самого начала сессии, task #26) — наконец
разобрана. Причина отрыва от места: у неё до сих пор жили `top:28px;right:-20px` от САМОЙ первой
версии дизайна («6.2»), где кнопка была `position:absolute` в углу. Более поздние слои («6.5», «7.0»)
переключили её на `position:relative` внутри `.nav-toolbar` рядом с «КАБИНЕТ», но старые смещения
`top`/`right` никто не обнулил — а `position:relative` эти смещения всё ещё применяет, просто
относительно новой (правильной) точки, а не абсолютно. В итоге кнопка визуально утекала на 28px вниз
и 20px вправо от места рядом с «Кабинет», зависая у первого пункта меню. Обнулил `top:0;right:0` в
финальном слое `.nav-toolbar .sidebar-toggle{}`. Заодно перерисовал сам значок — было «шеврон + отдельная
вертикальная палка» (то, что клиент описал как «кусок галочки с палкой»), стало единая иконка
«панель с раскрывающей стрелкой» (прямоугольник со внутренним разделителем и шевроном) — узнаваемый
паттерн «свернуть боковую панель», как в VS Code и похожих интерфейсах.

**4. Убрана дублирующая карточка «Последние изменения» со страницы Диагностики** — с тех пор как
это же появилось в выпадающей панели шапки (прошлый раунд), карточка на странице стала избыточной.
Убрал `compactHistory` из вывода `renderClientEdit` полностью (не просто скрыл — удалил и определение,
и использование, `changeHistoryRows()` при этом остаётся нужна другим местам, не трогал).

Проверено вживую всё вместе на моке с реалистичными данными (заявка с ошибкой домена + история
изменений одновременно) — пересечение по `elementFromPoint`, вертикальное выравнивание стрелки
с «Кабинет», клик сворачивает/разворачивает сайдбар, дублирующая карточка отсутствует. Тесты 144/144.

## «Проверяю…» зависает навсегда + доводка Главной по мокапу (2026-08-18, ночь, продолжение)

**Баг**: клиент нажал «Запустить диагностику» — кнопка ушла в «Проверяю…» и осталась так
навсегда. Разобрал через `wrangler tail` вживую во время воспроизведения: в логах `/inventory`
отработал, а `/check` за то же время в логах вообще не появился — то есть завис где-то в пути
(вероятно, реальный сайт ответил медленно, а `checkPlatformSite()` при не до конца готовых формах
дополнительно запускает свой собственный `scanSiteInventory(maxPages:40)`, то есть `/check` и
`/inventory` иногда независимо друг от друга гоняют по 40 страниц сайта — существовало и раньше,
я не трогал, но раньше кнопка не была на главном экране, поэтому реже давала о себе знать).

**Корневая причина, почему это выглядело как «зависло навсегда», а не «долго»**: у общей функции
`api()` (все сетевые запросы в клиенте идут через неё) вообще не было тайм-аута на `fetch()`.
Если сервер один раз не ответит (зависшее соединение, оборванный воркер) — промис просто никогда
не резолвится и не реджектится, `Promise.all([check,inventory])` в `runDiagnostics()` ждёт вечно,
`finally{diagnosticsRunning=false}` никогда не срабатывает. Добавил `signal:AbortSignal.timeout(45000)`
в `api()` — теперь любой зависший запрос гарантированно завершается ошибкой максимум через 45с,
кнопка разблокируется, показывается понятный тост «Сервер не ответил вовремя. Попробуйте ещё раз
через минуту.» вместо вечного «Проверяю…». Проверено не абстрактно, а против реально зависающего
(принимает соединение, никогда не отвечает) мок-эндпоинта — `fetch` корректно падает с
`TimeoutError` по истечении тайм-аута, ловится и превращается в понятный текст. Это чинит
конкретный репортнутый случай (сеть/зависший воркер), а не переделывает архитектуру двойного
сканирования — это отдельная, более крупная задача, которую здесь не трогал.

**Доводка Главной по мокапу**, раз клиент прямо попросил делать такой анализ самостоятельно, не
дожидаясь, пока укажут на каждую мелочь:
- Плитки статистики раньше были у всех одинаково белые, в макете у каждой лёгкий фоновый подтон
  в цвет своей иконки. Добавил `.stat-tile.purple/.red/.amber/.blue` с едва заметными тонированными
  фонами (`#faf9ff`/`#fffafa`/`#fffdf7`/`#f8fbff`) — заметно мягче самого бейджа иконки, но уже не
  одноцветно-белые.
- Иконка короны в карточке «SiteCare Pro» была символом Unicode (♛) — шрифтозависимый, на разных
  платформах/шрифтах может выглядеть по-разному или отсутствовать. Заменил на inline SVG, как и
  все остальные иконки в продукте.

Тесты 144/144.
